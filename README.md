# docker-jupyter

- generate docker image\

   ``` 
    docker build . -t docker-jupyter 
   ```

- start docker jupyter
   ```
    ./jupyter 
   ```

- open in browser

   ```
    http://localhost:8188
   ```

